import React from 'react';

import './loader.scss';

const Loader = (): JSX.Element => {
    return (
        <div className="loader">
            Cargando...
        </div>
    );
};

export default Loader;
